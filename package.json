{
  "name": "navkeys",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "set NODE_ENV=development && vite build --watch --mode development ",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "hot-reload-extension-vite": "^1.0.13",
    "typescript": "^5.2.2",
    "vite": "^5.1.0"
  }
}
